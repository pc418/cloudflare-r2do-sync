{
  "name": "cloudflare-r2do-sync-plugin",
  "private": true,
  "type": "module",
  "version": "0.7.3",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "test:live": "vitest run --config vitest.live.config.ts",
    "typecheck": "tsc --noEmit && tsc --noEmit -p test/live",
    "lint": "eslint .",
    "build": "node build.mjs",
    "validate": "node ../scripts/release-validate.mjs"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "@types/node": "^24.0.0",
    "esbuild": "^0.28.2",
    "eslint": "^10.8.1",
    "obsidian": "^1.7.2",
    "qrcode-generator": "^2.0.4",
    "typescript": "^5.6.0",
    "typescript-eslint": "^8.67.0",
    "vitest": "^4.1.10"
  },
  "overrides": {
    "nanoid": "^3.3.17"
  }
}
